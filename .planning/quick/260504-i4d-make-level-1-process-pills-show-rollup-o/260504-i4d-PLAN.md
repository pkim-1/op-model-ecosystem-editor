---
phase: quick
plan: 260504-i4d
type: execute
wave: 1
depends_on: []
files_modified: ["/Users/paulkim/Desktop/op-model-ecosystem-editor-LATEST.html"]
autonomous: true
---

<objective>
Make level 1 process pills highlight ALL teams (parent + children rollup) while keeping level 2 pills highlighting only their own teams.

Purpose: Parent pills should give a complete picture of all teams involved across the entire process hierarchy.
Output: Modified highlightTeamsForPill function with rollup behavior for parent pills.
</objective>

<context>
@/Users/paulkim/Desktop/op-model-ecosystem-editor-LATEST.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add team rollup logic for parent pill highlighting</name>
  <files>/Users/paulkim/Desktop/op-model-ecosystem-editor-LATEST.html</files>
  <action>
In the `highlightTeamsForPill(index, subIndex)` function (around line 2003), replace the team collection logic:

Current (line 2005-2007):
```javascript
const isSubPill = (subIndex !== undefined && subIndex !== null);
const target = isSubPill ? proc.children[subIndex] : proc;
const teamIds = new Set(target.teams);
```

Replace with:
```javascript
const isSubPill = (subIndex !== undefined && subIndex !== null);
let teamIds;
if (isSubPill) {
    // Child pill: only that child's teams
    teamIds = new Set(proc.children[subIndex].teams);
} else {
    // Parent pill: rollup of parent teams + all children's teams
    teamIds = new Set(proc.teams);
    if (proc.children && proc.children.length > 0) {
        proc.children.forEach(child => {
            if (child.teams) {
                child.teams.forEach(t => teamIds.add(t));
            }
        });
    }
}
```

No other changes needed. The explanation panel logic is already independent per level.
  </action>
  <verify>
Open the HTML file in a browser. Click a level 1 (parent) process pill that has children with different team assignments. Verify that ALL teams from parent AND children are highlighted. Then click a level 2 (child) pill and verify only that child's teams highlight.
  </verify>
  <done>Parent pills highlight the union of all teams across parent + children. Child pills still highlight only their own teams. Explanations remain unchanged.</done>
</task>

</tasks>

<verification>
- Click parent pill: highlights teams from proc.teams + all proc.children[].teams
- Click child pill: highlights only that child's teams (unchanged behavior)
- Explanation panel content unchanged for both levels
</verification>

<success_criteria>
Level 1 pills show rollup highlighting of all descendant teams. Level 2 pills remain scoped to their own teams only.
</success_criteria>
