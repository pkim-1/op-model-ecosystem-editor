---
phase: quick
plan: 260504-n0j
type: execute
wave: 1
depends_on: [260504-mbf]
files_modified:
  - /Users/paulkim/Desktop/op-model-ecosystem-editor-LATEST.html
autonomous: true
must_haves:
  truths:
    - "Clicking a sub-pill highlights connections and team cards in that sub-pill's unique color"
    - "Parent pill highlighting still uses default green (rollup behavior unchanged)"
    - "Both edit mode and present mode use colored highlighting for sub-pills"
  artifacts:
    - path: "Desktop/op-model-ecosystem-editor-LATEST.html"
      provides: "Color-matched highlighting for sub-pills"
      contains: "SUB_PILL_COLORS"
  key_links:
    - from: "highlightTeamsForPill()"
      to: "team-card.highlighted / conn-highlighted"
      via: "inline style override using sub-pill color palette"
      pattern: "subIndex.*color"
---

<objective>
Extend the sub-pill 8-color palette to connection line highlighting and team card highlighting, so that selecting a sub-pill visually connects its color through the entire UI (pill -> lines -> cards).

Purpose: Visual consistency -- users can instantly trace which teams and connections belong to a specific sub-process by color.
Output: Updated op-model-ecosystem-editor-LATEST.html with color-matched highlighting.
</objective>

<execution_context>
@/Users/paulkim/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paulkim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/paulkim/Desktop/op-model-ecosystem-editor-LATEST.html
@/Users/paulkim/.planning/quick/260504-mbf-add-unique-colors-to-sub-process-pills-f/260504-mbf-SUMMARY.md

<interfaces>
<!-- Sub-pill color palette (lines 750-757) -->
Color 0: rgba(66, 72, 237)    - Pacific Blue
Color 1: rgba(155, 81, 224)   - Purple
Color 2: rgba(0, 217, 165)    - Teal/Green
Color 3: rgba(255, 184, 0)    - Amber
Color 4: rgba(255, 107, 107)  - Coral
Color 5: rgba(72, 199, 235)   - Cyan
Color 6: rgba(255, 159, 67)   - Orange
Color 7: rgba(162, 217, 80)   - Lime

<!-- Current highlighting CSS (lines 650-668) -->
.team-card.highlighted { border-color: var(--accent-green); box-shadow: 0 0 20px rgba(0, 217, 165, 0.5); }
svg.connections path.conn-highlighted { stroke: var(--accent-green); stroke-width: 3; opacity: 1; }

<!-- highlightTeamsForPill function signature (line 2050) -->
function highlightTeamsForPill(index, subIndex)
- When subIndex is defined: highlights only that child's teams
- When subIndex is undefined: highlights rollup of parent + all children teams
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JS color palette array and modify highlightTeamsForPill to apply sub-pill colors</name>
  <files>/Users/paulkim/Desktop/op-model-ecosystem-editor-LATEST.html</files>
  <action>
1. Add a JS constant array near the top of the script section (after state initialization) containing the 8 RGB color values matching the CSS sub-pill-color classes:
```javascript
const SUB_PILL_COLORS = [
    [66, 72, 237],    // 0 - Pacific Blue
    [155, 81, 224],   // 1 - Purple
    [0, 217, 165],    // 2 - Teal
    [255, 184, 0],    // 3 - Amber
    [255, 107, 107],  // 4 - Coral
    [72, 199, 235],   // 5 - Cyan
    [255, 159, 67],   // 6 - Orange
    [162, 217, 80],   // 7 - Lime
];
```

2. Modify `highlightTeamsForPill(index, subIndex)` function (line ~2050):
   - After determining `teamIds`, check if `isSubPill` is true
   - If sub-pill: compute `const colorIdx = subIndex % 8;` and `const [r, g, b] = SUB_PILL_COLORS[colorIdx];`
   - In the team card highlight loop (line ~2074): when `isSubPill`, instead of just adding `highlighted` class, also set inline styles:
     ```javascript
     el.style.borderColor = `rgba(${r}, ${g}, ${b}, 0.8)`;
     el.style.boxShadow = `0 0 20px rgba(${r}, ${g}, ${b}, 0.5)`;
     ```
     Still add `highlighted` class for z-index and other non-color properties.
   - In the connection highlight loop (line ~2094): when `isSubPill`, after adding `conn-highlighted`, set inline style:
     ```javascript
     path.style.stroke = `rgba(${r}, ${g}, ${b}, 1)`;
     ```
   - If NOT sub-pill (parent): leave behavior as-is (uses CSS class defaults = green)

3. Modify `clearHighlighting()` function (line ~2103):
   - When removing `highlighted` from team cards, also clear inline styles: `el.style.borderColor = ''; el.style.boxShadow = '';`
   - When removing `conn-highlighted` from paths, also clear inline style: `path.style.stroke = '';`

This approach uses inline styles to override the CSS class defaults only when a sub-pill is selected, keeping parent pill behavior unchanged.
  </action>
  <verify>
    <automated>grep -c "SUB_PILL_COLORS" /Users/paulkim/Desktop/op-model-ecosystem-editor-LATEST.html | grep -q "1" && grep -c "colorIdx" /Users/paulkim/Desktop/op-model-ecosystem-editor-LATEST.html | grep -q "[1-9]" && grep "style.borderColor\|style.boxShadow\|style.stroke" /Users/paulkim/Desktop/op-model-ecosystem-editor-LATEST.html | grep -c "rgba" | grep -q "[2-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
- SUB_PILL_COLORS array exists with 8 entries matching the CSS palette
- highlightTeamsForPill applies sub-pill color to team cards and connection lines when subIndex is provided
- Parent pill highlighting (no subIndex) still uses default green via CSS classes
- clearHighlighting removes all inline color styles
- No regressions in edit or present mode pill clicking
  </done>
</task>

</tasks>

<verification>
Open op-model-ecosystem-editor-LATEST.html in a browser:
1. Switch to Present mode
2. Click a sub-pill -- verify team cards glow with that sub-pill's color (not green)
3. Verify connection lines between highlighted teams use that sub-pill's color
4. Click a different sub-pill -- verify color changes to match
5. Click the parent pill -- verify it uses default green highlighting
6. Switch to Edit mode and select a sub-pill -- verify same colored highlighting
7. Click away / deselect -- verify all highlighting clears cleanly
</verification>

<success_criteria>
- Sub-pill selection produces color-matched highlights on team cards and connections
- Parent pill selection still uses default green
- Colors are visually consistent (same hue on pill, lines, and card borders)
- No leftover inline styles after clearing
</success_criteria>

<output>
After completion, create `.planning/quick/260504-n0j-extend-sub-pill-color-coding-to-connecti/260504-n0j-SUMMARY.md`
</output>
