---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Desktop/op-model-ecosystem-editor-LATEST.html]
autonomous: true
must_haves:
  truths:
    - "User can import a JSON file to load a shared diagram"
    - "After import, the diagram renders the imported data"
    - "Existing Export JSON button still works for sharing"
  artifacts:
    - path: "Desktop/op-model-ecosystem-editor-LATEST.html"
      provides: "Import JSON button in toolbar"
      contains: "btnImportJson"
  key_links:
    - from: "Import button click handler"
      to: "localStorage.setItem + init()"
      via: "FileReader reads JSON, validates, stores, re-initializes"
      pattern: "localStorage\\.setItem.*STORAGE_KEY"
---

<objective>
Add an Import JSON button to the ecosystem editor toolbar so users can load shared diagram files.

Purpose: Enable sharing diagrams between people by importing exported JSON files.
Output: Updated HTML file with working import functionality.
</objective>

<context>
@Desktop/op-model-ecosystem-editor-LATEST.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Import JSON button and file input handler</name>
  <files>Desktop/op-model-ecosystem-editor-LATEST.html</files>
  <action>
1. Add an "Import JSON" button in the toolbar next to the existing "Export HTML" button (line ~869):
   ```html
   <button class="toolbar-btn" id="btnImportJson">Import JSON</button>
   ```

2. Add a hidden file input element after the toolbar:
   ```html
   <input type="file" id="fileImportJson" accept=".json" style="display:none">
   ```

3. Add event listener for btnImportJson that triggers the hidden file input click.

4. Add event listener for the file input change event that:
   - Reads the selected file with FileReader
   - Parses JSON and validates it has expected structure (checks for `cards` array)
   - If valid: calls `localStorage.setItem(STORAGE_KEY, JSON.stringify(parsed))` then calls `init()` to reload the diagram
   - If invalid: shows toast with error message "Invalid file format"
   - Resets the file input value so the same file can be re-imported

Place the event listeners near the existing Export JSON listener (around line 1632).
  </action>
  <verify>Open the file in browser. Click "Import JSON" button - file picker should appear. Select a previously exported JSON file - diagram should update to show imported data. Verify Export JSON still works.</verify>
  <done>Import JSON button visible in toolbar, clicking it opens file picker, selecting valid JSON loads diagram, invalid files show error toast.</done>
</task>

</tasks>

<verification>
- Open file in browser
- Export current diagram as JSON
- Modify something (move a card)
- Import the exported JSON
- Diagram reverts to exported state
- Invalid file (e.g. a .txt) shows error toast
</verification>

<success_criteria>
- Import JSON button present in toolbar with consistent styling
- File picker accepts .json files
- Valid import loads data and refreshes diagram
- Invalid import shows error message
- Export JSON continues to work unchanged
</success_criteria>

<output>
After completion, the updated file is ready to use at Desktop/op-model-ecosystem-editor-LATEST.html
</output>
